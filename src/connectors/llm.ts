// TEMPORARILY DISABLED: Architecture cleanup removed PRContext/LinearContext types
// TODO: Fix after decisions on connector architecture
// Will re-enable after core types are clarified

export function createLLMConnector() {
  throw new Error('LLMConnector temporarily disabled during architecture cleanup');
}
